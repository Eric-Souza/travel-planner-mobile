/**
 * API types aligned with travel-planner-api OpenAPI schema.
 * Regenerate via `npm run api:generate` when the backend is running.
 */
export type { components, paths, operations } from './api-types.generated';
